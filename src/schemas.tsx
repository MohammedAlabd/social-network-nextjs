export interface UserData {
  id: number;
  name: string;
  username: string;
  profilePicture: string;
  membership: string;
}
export interface SectionData {
  SectionName: string;
  SectionUrl: string;
  SectionPhoto: string;
}
